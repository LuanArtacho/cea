name: cea_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.8.1 <4.0.0' # Adicionei um limite superior para maior estabilidade

dependencies:
  flutter:
    sdk: flutter

    # Firebase - Usando as versões estáveis mais recentes
  firebase_core: ^3.1.1
  cloud_firestore: ^5.1.0

  fl_chart: ^0.66.2
  intl: ^0.19.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/